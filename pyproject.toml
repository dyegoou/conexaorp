[project]
name = "discord-bot"
version = "0.1.0"
description = "Bot profissional para Discord com slash commands e permissões por cargo."
requires-python = ">=3.13"
dependencies = [
    "discord-py>=2.7.1",
]

[project.scripts]
discord-bot = "discord_bot.main:main"

[tool.uv]
package = true
